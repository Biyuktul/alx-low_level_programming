#include "hash_tables.h"
#include <stdlib.h>

/**
 * hash_table_delete - deletes a hash table.
 * @ht: the hash table.
 * Return: Always void.
*/
void hash_table_delete(hash_table_t *ht)
{
        if (ht == NULL)
		return (0);
        
}