#include <stdio.h>
/**
 *main - Entry point
 *
 *Return: the alphabet except e and q in lowercase, followed by a new line
 **/

int main(void)
{
char num;

for (num = 0; num < 10; num++)
{
printf("%i", num);
}

return (0);
}
